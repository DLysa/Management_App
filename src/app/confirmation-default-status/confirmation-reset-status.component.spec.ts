/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ConfirmationResetStatusComponent } from './confirmation-reset-status.component';

describe('ConfirmationResetStatusComponent', () => {
  let component: ConfirmationResetStatusComponent;
  let fixture: ComponentFixture<ConfirmationResetStatusComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ConfirmationResetStatusComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ConfirmationResetStatusComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
